---
"@ngrok/mantle": patch
---

`Calendar` now merges `className` with `classNames.root` instead of letting a `classNames.root` replace it. Its JSDoc documents the `data-slot="calendar"` hook and states that it is a month grid with no text entry.
