---
"@ngrok/mantle": patch
---

`Table.Header` defaults `scope` to `"col"`. Pass `scope="row"` for a header cell inside a body row. The docs now say that `Table.Root` attaches `ref` to its inner scroll container and that `Table.Caption` is the table's accessible name.
