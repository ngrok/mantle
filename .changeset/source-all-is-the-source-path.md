---
"@ngrok/mantle": patch
---

Update the `source-all.css` header comment: it no longer recommends the retired `mantleTwSourcePlugin` for apps that use a subset of mantle. Import `source-all.css` alongside `mantle.css` to register mantle's Tailwind sources.
