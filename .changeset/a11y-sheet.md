---
"@ngrok/mantle": patch
---

`Sheet` JSDoc and docs now say the sheet slides in from the left or right edge. `side` accepts `"left" | "right"` only; the docs listed `"top"` and `"bottom"`, which the type never accepted.
