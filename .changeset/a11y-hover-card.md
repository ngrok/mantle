---
"@ngrok/mantle": patch
---

`HoverCard` JSDoc: the `Content` and `Portal` namespace summaries now match their parts, including that content mounts into the nearest layer container when `container` is omitted.
