---
"@ngrok/mantle": patch
---

`OtpInput.Root` documents its accessible-name contract. The hidden input needs a name: inside a `Field.Item`, `Field.Label` supplies it; on its own, pass `aria-label`. The docs examples that render the control on its own now do so.
