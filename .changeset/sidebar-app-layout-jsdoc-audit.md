---
"@ngrok/mantle": patch
---

Sidebar/AppLayout JSDoc audit: add the missing `@see`/`@example` blocks to the exported `SidebarAccount`, `SidebarMobileBreakpoint`, and `SidebarState` types; mirror the canonical full-tree examples onto the `Sidebar.Trigger`, `Sidebar.AccountAvatar`, `Sidebar.UserAvatar`, `AppLayout.Notice`, and `AppLayout.Content` part JSDoc; and complete the top-level `Sidebar` usage example with `Sidebar.Separator` and `Sidebar.Trigger`.
