---
"@ngrok/mantle": patch
---

`Empty.Title` is typed as `ComponentProps<"h3">`, so it accepts `ref` as the docs state. The docs now give `Empty.Icon`'s real size (`size-10`) and say the icon is hidden from assistive technology.
