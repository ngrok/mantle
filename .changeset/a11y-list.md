---
"@ngrok/mantle": patch
---

The `List.Root`, `List.VirtualRoot`, and `List.Item` summaries in the built types now match their declarations, and the props docs name `isItemDisabled` among the grid-only knobs that `List` omits.
