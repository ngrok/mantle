---
"@ngrok/mantle": patch
---

The search icon in `SelectableList.Filter` is hidden from assistive technology. Every `SelectableList` part summary in the built types now matches its declaration, so hover documentation and the docs page say the same thing.
