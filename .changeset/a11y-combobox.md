---
"@ngrok/mantle": patch
---

The `Combobox.Group`, `Combobox.GroupLabel`, and `Combobox.ItemValue` summaries in the built types now carry the same wrapping and `aria-labelledby` guidance as the docs page.
