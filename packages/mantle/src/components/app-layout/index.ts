/**
 * Re-exports for the AppLayout compound component — a viewport-locked
 * application shell with a full-width notice strip, an optional toolbar
 * header, and a rounded content card that is the shell's only scroll
 * container.
 *
 * @see https://mantle.ngrok.com/layouts/app-layout
 */

export {
	//,
	AppLayout,
} from "./app-layout.js";
