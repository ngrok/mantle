/**
 * Re-exports for the Sidebar compound component — a composable, collapsible
 * app-navigation sidebar with a desktop collapse-to-hidden panel, a mobile
 * `Sheet` presentation, and a toggle trigger that composes into any app shell.
 *
 * @see https://mantle.ngrok.com/components/navigation/sidebar
 */

export {
	//,
	Sidebar,
	useSidebar,
} from "./sidebar.js";

export type {
	//,
	SidebarAccount,
	SidebarCollapsible,
	SidebarMobileBreakpoint,
	SidebarState,
} from "./sidebar.js";
